import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'app-deputados',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
